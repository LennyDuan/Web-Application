<h2 class="total_h2">Cart</h2>
<table>
	<tr>
		<th>Product</th>
		<th>Num</th>
		<th>Price</th>
	</tr>
	<%= render (@cart.line_items)%>
	<tr class="total_line">
		<td colspan="2" class="total_cell">Total: </td>
		<td class="total_cell">£<%= @cart.total_price %></td>
	</tr>
</table>
<%= button_to "Check Out", new_order_path, method: :get %>
<%= button_to 'Empty cart', cart, method: :delete,
	data: { confirm: 'Are you sure?' } %>
