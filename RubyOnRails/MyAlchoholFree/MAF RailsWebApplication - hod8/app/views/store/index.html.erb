<% if notice %>
	<p id="notice"><%= notice %></p>
<% end %>

<h1 class="store">Wine Story
	</h1>
<% @products.each do |product| %>
	<div class="entry">
		<%= link_to image_tag(product.image_url), product_path(product.id)%>
		<h3><%= product.title%></h3>
		<h5><%= truncate(strip_tags(product.description), length: 170 ) %></h5>
		<h4 class="price_line">Company: <%= product.company%></h4444>
			<h4 class="price_line" >Size: <%= product.size%> ml</h4>
			<h4 class="price_line">
				<span class="price" ><%= sprintf("£ %0.02f", product.price) %></span>
				<%= form_tag(line_items_path(product_id: product.id), method:"create") do %>                                                    
					<%= hidden_field_tag :quantity, "1",  class:"quantity"%>
					<%= submit_tag("Add to Cart") %>
				<% end %>
			</h4>
	</div>

<% end %>
